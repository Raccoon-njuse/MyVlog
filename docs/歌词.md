# 歌词来源

当前系统不再在文档或管理后台维护歌词正文。

运行时歌词以 `storage/audios/` 中的 LRC 文件为唯一来源：

- `中文版.lrc`
- `英文版.lrc`

服务端会按中文版在前、英文版在后的顺序拼接歌词。第一条歌词来自中文版 LRC 的 `-----`，最后一条歌词来自英文版 LRC 的最后一句。

如果需要修改歌词、拆分句子或调整时间点，请直接更新对应 LRC 文件。LRC 内容指纹变化后，服务端会整体同步新歌词，并把现有视频歌词关联放入 Relink 暂存区。
