# AGENTS.md

## 提交与服务端更新规则

- 每次提交都必须使用中文约定式提交信息，并且不能只写一行标题；提交正文必须说明服务端更新方式。
- 如果本次变更不涉及数据库结构或数据迁移，提交正文必须明确写明：服务端 `git pull` 后是否只需要重启服务，且无需执行数据库迁移。
- 如果本次变更涉及数据库结构、字段、索引、枚举、种子数据或数据修复，必须同时满足：
  - 新增或修改 `migrations/*.sql`，迁移应尽量可重复执行或能被 `schema_migrations` 记录式迁移安全跳过。
  - 提交正文必须写明服务端操作命令，例如 `npm run db:migrate`。
  - 提交正文必须列出涉及的迁移文件名，例如 `003_remove_lyric_sections.sql`。
  - 提交正文必须说明数据影响范围，例如“不会覆盖歌词、视频或关联数据，只删除已弃用字段”。
  - 如果迁移可能删除、重写或批量修正已有业务数据，提交正文必须明确提醒先备份数据库。
- 不能只在聊天回复里说明迁移步骤；迁移脚本、README 或提交信息里至少要有一个可被服务端维护者在 `git pull` 后直接看到的操作说明。

## 数据库更新命令

服务端更新代码后，默认数据库迁移命令为：

```bash
npm run db:migrate
```

旧命令 `npm run db:init` 当前只是 `db:migrate` 的兼容别名，不应在新提交说明中优先使用。
